<!--index.wxml-->
<view class="container">
  <view class="one" wx:for="{{kk}}">
    <view>
     <image src="{{item.pic}}"></image>
    </view>
    <view>
      {{item.name}}
    </view>
    <view>
      {{item.price}}
    </view>
  </view>


</view>
